import Navbar from "./Navbar";

const Header = () => {
  return (
    <header>
      <div className="container" id="container">
        <Navbar />
      </div>
    </header>
  );
};

export default Header;
